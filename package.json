{
  "name": "trazapiezas",
  "version": "0.0.0",
  "scripts": {
    "db": "docker compose up -d",
    "front": "cd frontend && npm start",
    "back": "cd backend && npm run dev",
    "dev": "npm-run-all --parallel db front back"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
